{
  "name": "fhir-fe-poc-pages",
  "version": "0.1.0",
  "homepage": "https://tewhatuora.github.io/fhir-fe-poc-pages/",
  "private": true,
  "scripts": {
    "build": "rm -rf build && cd ../fhir-fe-poc-MOH && yarn build && rm -rf build/config && cp -R build ../fhir-fe-poc-pages-MOH",
    "deploy" : "gh-pages -d build"
  },
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
